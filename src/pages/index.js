import React from 'react';

import Layout from '../components/layout';
import Button from '../components/Button';

const IndexPage = () => (
    <Layout>
        <h1>SPENCER SNYDER</h1>
        <p>is the man</p>
        <p>Now go fuck up some tags.</p>
        <div>
            <Button>Button</Button>
            <Button>Button</Button>
            <Button>Button</Button>
        </div>
    </Layout>
);

export default IndexPage;
